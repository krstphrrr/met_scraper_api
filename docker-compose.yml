version: '3'
services:
  # node:
  #   image: ng_test
  #   ports:
  #     - 4200:4200
  rabbit:
    hostname: rabbit
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"
    expose:
      - "5672"

  worker:
    image: celery
